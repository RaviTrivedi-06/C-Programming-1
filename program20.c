#include <stdio.h>
    int main()
            {
            float H;
        printf("Enter the height of the triangle : ");
        scanf("%f",&H);

        float L;
        printf("Enter the corresponding base length of the triangle : ");
        scanf("%f",&L);



        float A = 0.5*L*H;


       printf("Area of the given triangle is : %f",A);

       return 0;

            }
